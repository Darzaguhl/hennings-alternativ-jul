// Default API base URL for local development and as a fallback if a
// deploy doesn't override it. Render's Build Command overwrites this file
// per-environment (see render build settings), so main/production can stay
// identical commits while still hitting different APIs.
window.API_BASE_URL = "https://hennings-alternativ-jul-api-preprod.onrender.com";
